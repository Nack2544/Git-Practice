new-item test.txt # creates new item on windows
touch test.txt # creates new item on linux
ctrl-s # SAVES!!!!!!!!!!!!!!
ctrl-z # undo
ctrl-c # copy
ctrl-v # paste
ctrl-a # highlight everything

git config --global unit.defaultbranch main # change the branch to main
git status # check the status 
git branch -m master main # change bracnh from master to main

-- add -- commit -- pull -- push -- 


git add . # to update the recent update on the file 
git commit -m " " # to commit a new commit 
git checkout -b sjinaongkan # create checkout branch and switch to that branch name sjinaongkan
git checkout main # change branch back to main

git branch # to check your current branch

 git remote add origin # to add the local folder to github

 git remote remove origin

 # to move the local files to git hub

 1: git remote add origin "link from github"
 2: git remote -v
 3: git push -u origin main

 -u option automatically sets that upstream for you, linking your repo to a central one. That way, in the future, Git "knows" where you want to push to and where you want to pull from, so you can use git pull or git push without arguments. in the waiting room